# Root environment variables (reference only — copy to each app as needed)
# See apps/web/.env.example, apps/api/.env.example, apps/ai-service/.env.example

DATABASE_URL="postgresql://jobmatch:jobmatch_dev@localhost:5434/jobmatch?schema=public"
REDIS_URL="redis://localhost:6379"
OLLAMA_API_BASE="http://localhost:11434"

# Resume uploads (local default — see ADR-010)
UPLOAD_DIR="./uploads"
